# -*- mode: snippet -*-
# name : hook_theme
# contributor : Stefan Freudenberg <s.freudenberg@jpberlin.de>
# --
/**
 * Implements hook_theme().
 */
function `(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))`_theme(\$existing, \$type, \$theme, \$path) {
return array(
$0
);
}
